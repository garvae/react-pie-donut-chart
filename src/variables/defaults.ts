/** correction ratio when passed only one data item */
export const SINGLE_VALUE_CORRECTION_RATIO = 0.99999999;

/** default focused segment stroke width to size ratio */
export const DEFAULT_FOCUSED_SEGMENT_STROKE_WIDTH_TO_SIZE_RATIO = 0.0066;

/** default outline color of focused segment */
export const DEFAULT_COLOR_SEGMENT_FOCUSED_OUTLINE = '#287bc8';

/** default chart center color */
export const DEFAULT_CHART_CENTER_COLOR = '#ffffff';

/** default chart text color */
export const DEFAULT_CHART_TEXT_COLOR = '#333333';

/** default chart segment scale ratio */
export const DEFAULT_CHART_SEGMENT_SCALE_RATIO = 1.05;

/** default resize re render debounce time */
export const DEFAULT_RESIZE_RE_RENDER_DEBOUNCE_TIME = 0 /* ms */;

/** default animation speed */
export const DEFAULT_ANIMATION_SPEED = 200 /* ms */;
